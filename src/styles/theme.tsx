import { createTheme } from '@mui/material'

export const theme = createTheme({
  palette: {
    primary: {
      main: '#f2f2f2',
      contrastText: '#2A2D34',
    },
    secondary: {
      main: '#2A2D34',
    },
  },
})

export default theme
